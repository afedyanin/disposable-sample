<?xml version="1.0" encoding="utf-8" ?>
<configuration>
    <startup> 
        <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.5" />
    </startup>
  <connectionStrings>
    <!-- 
    The project expects to find a local SQL Server database with a default, unnamed instance. 
    If your SQL Server setup is different, you will need to change the connection string in app.config.
    -->
    <add name="master"
         connectionString="Server=.;Database=master;Integrated Security=SSPI;App=GetDate.ConsoleApp;"/>
  </connectionStrings>
</configuration>